#include "main.h"
/**
 * _isupper - checks for uppercase
 * @c: upper case character
 * Return: 1 if c is uppercase, 0 if otherwise
 */
int _isupper(int c)


{
	if (c >= 'A' && c <= 'z')

	{
		return (1);

	}
	else
	{
		return (0);
	}
	putchar('\n')
}
